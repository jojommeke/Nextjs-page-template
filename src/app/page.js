import React from 'react'
import Image from 'next/image'
import { Kranky } from 'next/font/google'

export default function main() {
  return (
    /* sample jsx, delete this to the comment after this to replace with your own */
    <div id="example" className="w-[100vw] h-[100vh] flex justify-center justify-items-center bg-black text-white">
      <p className="font-extrabold m-auto text-6xl hover:text-7xl">example</p>
    </div>
    /* delete/replace till here */
  );
}
